<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 背景 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="0dip" />

            <gradient
                android:angle="270"
                android:centerColor="#fafafa"
                android:centerY="0.75"
                android:endColor="#fafafa"
                android:startColor="#fafafa" />
        </shape>
    </item>

    <!--
    第二进度条
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="0dip" />

                <gradient
                    android:angle="270"
                    android:centerColor="#84bedb"
                    android:centerY="0.75"
                    android:endColor="#84bedb"
                    android:startColor="#84bedb" />
            </shape>
        </clip>
    </item>
    -->


    <!-- 第一进度条 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="0dip" />

                <gradient
                    android:angle="270"
                    android:centerColor="@color/color_full_scan_text"
                    android:centerY="0.75"
                    android:endColor="@color/color_full_scan_text"
                    android:startColor="@color/color_full_scan_text" />
            </shape>
        </clip>
    </item>

</layer-list>