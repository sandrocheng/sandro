<?xml version="1.0" encoding="utf-8"?>
<!-- joy 图片导入页面 gridview item -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center"
    android:orientation="vertical" >

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" >

        <!-- 图片imageView -->

        <com.tencent.onesecurity.safebox.ui.adapter.CustomImageView
            android:id="@+id/photo_img_view"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop" />
        <!-- 图片相册背景 -->

        <ImageView
            android:id="@+id/photo_img_bg"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:src="@drawable/sel_vault_photos_bg" />
        <!-- 图片相册编辑状态下背景 -->

        <ImageView
            android:id="@+id/photo_img_check_bg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/btn_vault_photos_photo_select"
            android:visibility="gone" />
        <!-- 图片相册编辑状态下CheckBox -->

        <ImageView
            android:id="@+id/photo_img_check"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|right"
            android:layout_marginBottom="9.00dp"
            android:layout_marginRight="8.35dp"
            android:src="@drawable/sel_cb_common_blue" />
    </FrameLayout>

</RelativeLayout>